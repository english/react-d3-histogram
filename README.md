# React + D3 Histogram

Histogram component entirely rendered by React, using D3 to compute sizes, positions etc.

## Example

![Example histogram](/../master/screenshot.png?raw=true)

## Build

sh build.sh
